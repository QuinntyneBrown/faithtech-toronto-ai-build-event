@startuml
title Review, apply and reconcile operator operations - context
!include <C4/C4_Context>
LAYOUT_WITH_LEGEND()
Person(op, "Database operator", "Runs approved local operations")
System(cli, "FaithTech operator CLI", "Production approval and attributable recovery")
System_Ext(platform, "FaithTech event platform", "Azure SQL data and API consumers")
Rel(op, cli, "Uses", "Terminal")
Rel(cli, platform, "Reads or changes persisted data", "Encrypted SQL")
@enduml
