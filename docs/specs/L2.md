# L2 - Detailed Requirements

## Reading and verification conventions

[L1.md](L1.md) defines scope, approved defaults, and mandatory ATDD conventions. Each requirement below traces to exactly one L1. Unless stated otherwise, participant actions require an active registration in the selected event and administrator actions require the admin role. Events isolate all participant and activity data.

**Provenance:** Product capabilities come from [the prompt](../prompt.md) and the explicit decisions listed in L1. Numerical limits, recovery behavior, and precise boundary rules are specification defaults that make those decisions testable, not claims that those details appeared in the prompt or were individually confirmed. The [review record](REVIEW.md) identifies new defaults and corrections. Actual roster entries, credentials, content, prizes, projects, map coordinates, and venue address are administrator-supplied event data, not invented seed data; L2-008 explicitly supplies editable informational preset content only.

**Cross-cutting acceptance:** Every UI-facing requirement must also pass L2-035 through L2-037 at its relevant screen, including loading, empty, success, validation, and failure states. Every protected action must satisfy the applicable actor and operation rules in L2-038 through L2-042; participant actions do not require the admin role. Security and responsiveness criteria apply even when not repeated under a feature.

**Terminology:** An active registration is a roster entry not deactivated by an administrator. A stage is one timed screen entry; a named phase can contain several stages. The selection stage/window is one event-level interval permitting participant team/project changes, independent of which screen is current. Stage changes do not imply project progress. Event project descriptions are shared choices; an event/team/project pairing stores that team's repository/demo links. Core activities remain accessible through event navigation when a scheduled screen changes, but time-limited mutations remain governed by their server-side windows.

## L2-001: Independent event configuration

**Traces to:** L1-001

Administrators must create and edit events with a title, timezone, start/end, venue name and location, logo, participant-facing content and links, and team/project/activity settings. Incomplete configuration must be saveable as a draft; only published events admit participants. A publishable event must have a title, valid schedule, venue location, and logo.

Drafts may omit publication fields; supplied values must still satisfy syntax, size, and applicable interval validation. Publication additionally requires a venue name, a textual address or valid latitude/longitude pair, an accepted logo, and resolved event/stage instants. An empty stage list is valid and uses configured waiting content. Administrators must edit waiting content, closing content, and an optional HTTPS directions link. Publishing is an explicit action, separate from draft saving; this version does not add event deletion or unpublishing.

**Acceptance Criteria:**

1. Given an administrator and two events, when content or settings are saved in one event, then reloading shows those values only in that event.
2. Given missing publication fields, when publication is requested, then field-specific errors are shown and the event remains a draft; draft saving still succeeds.
3. Given a published event, when an administrator saves valid content or link changes, then connected clients show the new values within two seconds without signing in again.
4. Given an invalid edit to a published event, when saving is attempted, then the last valid configuration remains active and the administrator sees the validation errors.
5. Given a draft missing venue and schedule fields, when it is saved, then the draft persists; when publication is requested, then each missing publication field is identified and participants remain unable to enter.
6. Given malformed supplied URLs or a complete but overlapping stage interval, when a draft is saved, then the invalid supplied values are rejected without losing the previously saved draft.
7. Given valid waiting/closing content and directions saved by an administrator, when the corresponding participant screen opens, then it displays that event's saved values and the directions action remains independent of the map.

---

## L2-002: Participant roster and credentials

**Traces to:** L1-001

Administrators must add, rename, deactivate, and reactivate event roster entries and issue or replace individual entry codes. Names need not be unique; participant identity must not depend on a name. A replacement code must invalidate the prior code and sessions without deleting participation history.

**Acceptance Criteria:**

1. Given two participants with the same name, when both are added, then each receives a distinct identity and credential and retains separate activity results.
2. Given an existing participant, when their display name changes, then their team, submissions, messages, and wins remain attached to the same identity.
3. Given a deactivated entry, when its credential or existing session is used, then event access is denied; reactivation restores access to its retained history.
4. Given a lost code or incorrect bound email, when an administrator replaces the code and explicitly clears the email binding if needed, then the old code and sessions fail and only the new code can establish access.

---

## L2-003: First access and email binding

**Traces to:** L1-002

The access screen must accept an email and individual entry code for the selected event. First successful access must atomically bind a trimmed, case-insensitive email to that roster entry. Each normalized email must identify at most one active roster entry per event. No email-verification or email-delivery service is required by this version.

**Acceptance Criteria:**

1. Given a published event and an unclaimed active entry, when a valid email and its code are submitted, then one binding is saved and the participant reaches the screen appropriate to the current event time.
2. Given an already bound entry, when the matching email with different casing or surrounding spaces and valid code are submitted, then access resumes as the same participant without creating another registration.
3. Given two concurrent first-access attempts using different emails for one code, when processed, then exactly one binding succeeds and the other is denied without changing the winner's binding.
4. Given a malformed email, duplicate event email, wrong-event code, revoked code, or mismatched bound email, when submitted, then access is denied without disclosing another participant's name or email; field syntax errors can identify the malformed field.

---

## L2-004: Session continuity and sign-out

**Traces to:** L1-002

Participant sessions must survive refresh and remain valid for 24 hours after authentication unless revoked. Participants must be able to authenticate again after the event to view its recap. Sign-out must revoke the current session and clear private client state.

**Acceptance Criteria:**

1. Given a valid session, when the participant refreshes or opens the event again, then the same identity and current event state are restored without code re-entry.
2. Given a signed-out, expired, or revoked session, when a protected screen or action is requested, then access requires authentication and cached private content is not displayed.
3. Given a completed event and active registration, when the participant authenticates again, then the recap opens without reopening closed team selection or quizzes.

---

## L2-005: Countdown and venue display

**Traces to:** L1-003

Before the configured start, authenticated participants must see the event title, venue name, configured logo, location map, textual address or coordinates, and a nonnegative days/hours/minutes/seconds countdown derived from server time. Configured external directions must remain usable independently of the embedded map.

**Acceptance Criteria:**

1. Given an event starting in 90 seconds, when its countdown is viewed, then the remaining time is displayed and decreases without a page reload; changing the device clock does not change the authoritative start.
2. Given a reachable configured map and logo, when the screen loads, then the map marks the configured location and the logo has an accessible venue label.
3. Given a failed map or logo load, when the countdown renders, then the event title, textual location, available directions link, and countdown remain usable with an explanatory fallback.
4. Given a connected countdown client, when server time reaches the event start, then the live screen opens within two seconds without a participant action and the countdown never becomes negative.

---

## L2-006: Schedule validation and exact boundaries

**Traces to:** L1-004

Administrators must configure event and stage start/end times, named stages, and their screen content or activity. Start is inclusive and end exclusive. Stages must have positive durations, be within the event interval, and not overlap. Event timezone and offset must resolve each scheduled instant unambiguously.

Administrators must configure a positive selection window and a positive demo-presentation interval within the event, or explicitly disable each activity. Disabled selection permits administrator assignment only. Window boundaries need not match individual screen boundaries. All scheduled values must include local dates as well as times so overnight events are unambiguous. Event, stage, and activity interval edits must be validated and saved together; a dependent invalid interval must identify its conflict without partially activating the edit. A nonexistent local time cannot be repaired by supplying an offset: it must be replaced with a valid local time; an ambiguous time requires one of its valid offsets.

**Acceptance Criteria:**

1. Given invalid ordering, overlapping stages, duplicate starts, or a stage outside the event interval, when saved, then the invalid schedule is rejected with the offending intervals identified.
2. Given adjacent stages A ending and B starting at 18:05, when server time reaches 18:05, then B is current; an action window ending then is closed, while an independent window spanning both stages remains open.
3. Given a gap between stages, when server time enters it, then waiting content and the next stage time appear; after the last stage but before event end, waiting content shows the event end instead of inventing a next stage.
4. Given an ambiguous time without a valid resolving offset or a nonexistent local time, when saved, then the schedule is rejected with the affected timezone field identified.
5. Given selection from 18:05 to 18:15 and screen entries starting at 18:05 and 18:10, when a participant selects at 18:10, then selection remains permitted; at 18:15 a new selection is rejected.
6. Given an event crossing midnight, when dated intervals are valid and saved, then stages follow their resolved instants in order rather than comparing time-of-day strings.

---

## L2-007: Live stage transitions and content

**Traces to:** L1-004

The client must follow the server's current configured stage automatically, including on initial login, refresh, resumed tabs, and reconnection. Administrators must be able to configure timed informational screens and interactive activities within the evening, including multiple screens within a named phase by using consecutive schedule entries. Saved schedule edits must take effect atomically.

Automatic replacement applies to the main event screen. Browsing another core activity must preserve the participant's view and draft while announcing and linking to the new current stage. Each timed entry has its own identity, content, and optional activity reference; two consecutive entries using the same screen type must still display their respective content. Waiting and closing content use the event configuration from L2-001. Screen transitions must not require discarding a draft to access the current activity.

**Acceptance Criteria:**

1. Given a connected participant, when a stage starts or an administrator changes the valid current schedule, then its screen appears within two seconds without a reload.
2. Given a suspended tab or disconnected participant who missed three stages, when connection resumes, then the current screen appears without replaying missed activities or animations.
3. Given unsubmitted profile or message text, when the scheduled screen changes, then that draft remains available in the same browser session; a quiz closing does not submit an unanswered question automatically.
4. Given a participant viewing profiles, chat, or project details, when a stage changes, then the client announces the current activity and makes it accessible immediately; returning to the event screen shows the current stage rather than a stale one.
5. Given adjacent informational entries of the same screen type, when their boundary is reached, then the second entry's title, content, and links replace the first entry's within two seconds.

---

## L2-008: September 9 reference configuration

**Traces to:** L1-004

The platform must provide an editable reference event for September 9, 2026, 17:00-21:00 America/Toronto at Stone Church, with Use Liturgy disabled. The presentation supplies these contiguous phase intervals: Gather 17:00-17:28, Discover 17:28-18:05, Discern 18:05-18:15, Develop 18:15-20:10, Demonstrate 20:10-20:50, and Send 20:50-21:00.

The following editable entries transcribe the presentation's distinct screen timestamps, with same-time content combined. Times are September 9, 2026, America/Toronto (UTC-04:00); each entry ends at the next listed start, and closing ends at 21:00. Informational material is guidance, not automation of agents or Liturgy work. Presenter notes and example project pitches do not provision actual participants, projects, credentials, prizes, or venue location. The approved preset requires administrator-confirmed location even though the deck contains an address.

| Start | Phase | Screen content / activity |
| --- | --- | --- |
| 17:00 | Gather | Arrival and welcome information |
| 17:20 | Gather | Welcome and opening prayer |
| 17:22 | Gather | Tonight's outcome and milestones |
| 17:28 | Discover | Introductions |
| 17:40 | Discover | The 4D cycle |
| 17:45 | Discover | The five R's |
| 17:50 | Discover | Project one presentation |
| 17:55 | Discover | Project two presentation |
| 18:05 | Discern | Lament, then discern; team and project selection |
| 18:10 | Discern | Scope down; team and project selection remains open |
| 18:15 | Develop | Setup sprint and building guidance |
| 18:30 | Develop | Write the mini-PRD |
| 18:40 | Develop | Build the specs; pizza and four ideas |
| 19:10 | Develop | Read what the agent wrote |
| 19:20 | Develop | Mocks and detailed design; stand up and network |
| 19:35 | Develop | Resolve the drift |
| 19:45 | Develop | Build incrementally, including the 20:00 reminder |
| 20:10 | Demonstrate | Record the demo; write your 60 seconds |
| 20:30 | Demonstrate | Team demos |
| 20:50 | Send | Raffle |
| 20:55 | Send | What's next, thanks, and closing prayer |

Selection is enabled for 18:05-18:15 and demo presentation for 20:30-20:50. The 20:10 preparation entry is not a presentation slot. At 21:00 the recap is default. The preset includes no invented quiz: administrators can configure one using L2-017 without shifting the reference timetable implicitly.

**Acceptance Criteria:**

1. Given the reference configuration, when inspected, then demos begin at 20:30, the raffle screen at 20:50, and closing content at 20:55; Discover and Discern gates in Liturgy are not changed at any boundary.
2. Given duplicate presentation timestamps, when the reference schedule is created, then their content is combined into one screen at that instant; consecutive unique timestamps inside a phase define consecutive screens ending at the next screen or phase boundary.
3. Given the Discern interval, when participants view it, then team and project selection are available from 18:05 until 18:15; the Develop screen retains building guidance and project repository/demo links.
4. Given the preset without an administrator-supplied logo and precise location, when saved, then it remains a draft and does not invent an address; supplying the required publication data permits publication.
5. Given another event created from the reference configuration, when its date, content, or settings change, then the September 9 event is unchanged and no participant identities, chat, submissions, or wins are copied.
6. Given the unedited preset, when its timed content is inspected, then all entries above are present in order, including both pieces of content at 17:20, 18:40, 19:20, and 20:10; untimed closing artwork does not add another stage.

---

## L2-009: Participant-selected teams

**Traces to:** L1-005

Administrators must configure team names and the random or participant-selected assignment mode. In participant-selected mode, active participants must join, leave, or switch among configured teams during the selection stage. There is no team capacity limit in this version. Administrators must be able to correct assignments after selection closes.

**Acceptance Criteria:**

1. Given an open selection stage in participant-selected mode, when an unassigned participant joins a team, then that team and its membership are displayed and the participant has exactly one membership.
2. Given a participant already on team A, when they switch to B, then the change removes A membership and adds B membership atomically; leaving instead makes them unassigned.
3. Given concurrent join/switch requests, when processed, then the participant ends with at most one team and clients show the saved assignment.
4. Given random mode or a closed selection stage, when a participant attempts to change membership, then the change is denied with an explanation; an administrator can still correct the assignment without altering quiz or raffle history.

---

## L2-010: Balanced random assignment

**Traces to:** L1-005

In random mode, an administrator must trigger assignment of all active roster entries, including those not yet signed in, across configured teams. A fresh assignment must shuffle participants and produce team counts differing by at most one. Later newly added participants must be assigned to a smallest team when the administrator requests assignment of unassigned entries; existing assignments remain unchanged.

**Acceptance Criteria:**

1. Given ten active participants and three empty teams, when random assignment runs, then all ten are assigned exactly once with team counts 4, 3, and 3 in some order.
2. Given existing assignments and two newly added participants, when unassigned entries are assigned, then existing memberships are preserved and each new participant joins a currently smallest team.
3. Given no teams or no eligible unassigned entries, when assignment is requested, then an actionable empty-state message is shown and no existing membership changes.
4. Given a repeated request after its response was lost, when retried, then saved assignments are returned without reshuffling participants.

---

## L2-011: Project choices and proposals

**Traces to:** L1-005

Administrators must configure either predefined-only project selection or a mode also allowing participant proposals. Projects must have a title and description; accepted proposals must immediately become event choices without a separate approval workflow. One project can be selected by multiple teams; each team has at most one selected project. Any team member can select or change its project during selection.

**Acceptance Criteria:**

1. Given predefined-only mode, when participants view choices or attempt to submit a proposal directly, then only administrator-defined choices are available and the proposal attempt is denied.
2. Given proposals enabled and selection open, when a participant submits a nonblank title and description, then the project becomes visible to the event and can be selected without a Liturgy URL or account.
3. Given a team member selecting a project, when the save succeeds, then every team member sees that one selected project; a concurrent stale selection is rejected and refreshed rather than silently replacing a newer selection.
4. Given an unassigned participant, a project from another event, or closed selection, when team selection is attempted, then the change is denied; administrators can correct the selection afterward.

---

## L2-012: Project details and building guidance

**Traces to:** L1-005

Project-selection and project-detail screens must show configured descriptions, selected teams, building guidance, and available repository/demo links. Administrators must customize guidance and tools links without requiring project-management features in this platform.

**Acceptance Criteria:**

1. Given a selected project with guidance and build-tool links, when a team views its details during Develop, then that guidance and the configured links remain available independently of Use Liturgy.
2. Given no selected project or no configured projects, when the participant opens project details or choices, then an explanatory empty state identifies the next allowed action rather than showing a broken page.
3. Given an administrator editing guidance, when the save succeeds, then participants receive the updated text without losing team membership or selected projects.
4. Given a project selected by a team, when its removal is attempted, then removal is rejected until administrators explicitly reassign or clear the affected selections.

---

## L2-013: Participant profiles

**Traces to:** L1-006

Participants must edit an event-specific profile containing an optional biography, skills, interests, and looking-for-help tags. The roster name identifies the participant. Profile sharing must default off; choosing to share makes those fields visible only to authenticated participants in that event. Email must never become a public profile field.

**Acceptance Criteria:**

1. Given a participant with no optional fields, when they skip profile creation, then every other core event feature remains available.
2. Given a profile owner, when they save shared profile fields, then same-event participants can view them; participants in other events cannot.
3. Given a shared profile, when the owner disables sharing or clears a field, then subsequent profile reads and recommendations no longer expose that content.
4. Given another participant's profile, when a participant attempts to edit it, then the server denies the change and the saved profile is unchanged.

---

## L2-014: Explainable people recommendations

**Traces to:** L1-006

Recommendations must rank other active, sharing participants in the same event by the number of distinct shared normalized tags across skills, interests, and help tags. Normalization trims whitespace and ignores case. Return at most five matches, exclude self and blocked participants in either direction, and break equal scores by display name then stable participant identity. No external AI service is required.

**Acceptance Criteria:**

1. Given candidates sharing three, two, and zero tags, when recommendations load, then the three-tag candidate precedes the two-tag candidate and the zero-tag candidate is excluded, with shared tags explaining each result.
2. Given repeated or differently cased versions of the same tag, when scores are calculated, then that tag contributes one match only.
3. Given no shared tags, no shared profile, or no eligible candidates, when recommendations load, then the client explains that no matches are available without inventing similarities.
4. Given a profile becoming private or a block being added, when recommendations refresh, then the affected candidate is absent; profiles from another event never enter the candidate set.

---

## L2-015: Private participant chat

**Traces to:** L1-006

Active participants must exchange persistent, text-only, one-to-one messages with other active participants in the same event. Messages must contain 1-2,000 characters after trimming. Conversations must show sender, server timestamp, chronological order, and sending/sent/failed state. Attachments, group chat, and cross-event conversations are outside this version.

**Acceptance Criteria:**

1. Given two unblocked same-event participants, when a message is accepted, then both can see it within two seconds while connected and can read the saved conversation after refresh.
2. Given a blank message, a message over 2,000 characters, self-recipient, or other-event recipient, when sent, then it is rejected without adding a conversation message.
3. Given a lost response after a message was saved, when the sender retries that send, then exactly one message appears; two intentionally separate sends with identical text remain two messages.
4. Given a disconnected sender, when sending fails, then the draft and failed state remain visible with an explicit retry action; the client does not claim delivery or silently send the draft on reconnection.

---

## L2-016: Blocking, reporting, and moderation

**Traces to:** L1-006

Participants must block or unblock another participant and report a specific received message with an optional reason. Either-direction blocking must prevent new messages between the pair and exclude recommendations, without deleting history. Administrators must review reports with the reported message and reason and resolve them or deactivate an abusive participant; ordinary chat history is not an administrator browsing feature.

**Acceptance Criteria:**

1. Given A blocks B, when either attempts a new message to the other, then delivery is denied; existing conversation history remains visible to its parties.
2. Given A removes their block and no reverse block exists, when either sends a valid message, then delivery succeeds again.
3. Given a received message, when its recipient reports it, then a report is saved for administrator review; reporting an inaccessible message is denied and retrying the same report does not duplicate it.
4. Given an administrator resolving a report, when a resolution is saved or the reported participant is deactivated, then the outcome is recorded and deactivation immediately prevents subsequent authenticated actions.

---

## L2-017: Quiz configuration and lifecycle

**Traces to:** L1-007

Administrators must create event quizzes with an ordered set of single-choice questions, each with 2-6 nonblank options and exactly one correct option. Quizzes must have explicit opening and closing times within the event; the quiz stage must expose the active quiz. Questions and answer keys must become immutable once the quiz opens.

Each quiz must have an event-local identity and title. Administrators must be able to save an incomplete inactive draft, add/remove/reorder its questions, and explicitly activate it after validation. An activated quiz opens at its inclusive opening instant and closes at its exclusive closing instant. Activation must occur before opening. Activated quiz windows must not overlap within an event. A quiz screen entry must reference one activated quiz in the same event and fit within its answer window; activating the quiz and linking its stage can be saved atomically. Navigation lists activated quizzes as upcoming, open, or closed; inactive drafts are administrator-only. Opening time, questions, order, and answer keys are fixed once opening occurs; closing-time changes must obey L2-006's closure rules.

**Acceptance Criteria:**

1. Given an empty quiz, blank question, invalid option count, or missing/multiple correct options, when activation is requested, then validation identifies the issue and the quiz remains inactive.
2. Given a valid scheduled quiz, when its opening time arrives, then participants can answer; at the closing instant new answers are rejected and results become available.
3. Given a quiz already open or with submissions, when an administrator tries to alter questions or correct answers, then the edit is rejected without affecting saved answers.
4. Given a participant requesting quiz content before closing, when the server responds, then no correct-option metadata or other participants' answers are disclosed.
5. Given an inactive incomplete quiz, when it is saved, then it remains editable and invisible to participants; attempting activation with invalid questions, overlapping times, or an invalid stage reference returns specific errors without partial activation.
6. Given quizzes A and B at separate times, when their referenced stage entries become current, then each opens the correct quiz; submissions and results for A never appear as B's data.
7. Given an activated upcoming quiz, when a participant opens it before its opening time, then its title and opening time appear without an enabled answer action; an empty quiz list explains that none are scheduled.

---

## L2-018: Quiz answer submission

**Traces to:** L1-007

Each participant must submit at most one answer per question while its quiz is open. The server's receipt time determines eligibility; viewing a question earlier does not reserve a submission. Confirmed answers must be immutable and persist across refresh.

Participants must navigate questions in their configured order and be able to revisit or skip questions while open. Submission feedback must confirm only the saved choice, never correctness or score before closing. After the last question, show submission progress and closing time until results are available. Refresh must restore saved choices; returning to an answered question must not enable replacing its answer. A participant cannot reset submissions or restart for another score.

**Acceptance Criteria:**

1. Given an open quiz and an unanswered question, when a valid option is submitted, then one answer is saved and shown as submitted after refresh.
2. Given an existing answer, when a different option is submitted or two different answers arrive concurrently, then at most one answer is accepted and the saved answer is returned with an explanation.
3. Given an exact retry of an accepted answer, when processed, then it returns the saved submission without awarding additional points.
4. Given a not-yet-open or closed quiz, or an option belonging to another question or event, when submitted, then no answer is saved and the participant sees why the submission was refused.
5. Given an accepted choice while the quiz remains open, when its confirmation appears or the question is revisited, then the saved choice is shown without correctness, points, or an enabled replacement action.
6. Given skipped questions and a still-open quiz, when the participant navigates back, then unanswered questions remain available; reaching the last question does not close the quiz or reveal results.

---

## L2-019: Quiz scoring and results

**Traces to:** L1-007

Closed quizzes must award one point per correct answer and zero per incorrect or unanswered question. Participants must see their answers, correct options, total score, and a same-event leaderboard for that quiz containing only display names, scores, and ranks. Leaderboards include participants with at least one submitted answer to that quiz and use competition ranking: 1, 1, 3 for a two-way first-place tie. No aggregate cross-quiz score is required. Equal-rank rows use the directory's name/identity ordering for display only. Participants with no answers see a personal zero score but do not enter the leaderboard.

**Acceptance Criteria:**

1. Given two correct, one incorrect, and one unanswered question, when the quiz closes, then the participant's result is 2 out of 4 and identifies each answer's outcome.
2. Given participant totals 3, 3, and 2, when results load, then their ranks are 1, 1, and 3; equal scores are never broken by response speed.
3. Given a closed quiz, when results refresh or are reopened, then scores remain unchanged and other participants' email addresses and individual answer histories remain private.
4. Given a participant with no answers and a quiz with no submissions, when it closes, then the personal result shows zero out of the question count with unanswered outcomes and the leaderboard shows an explicit no-submissions state.

---

## L2-020: Raffle prizes and eligibility

**Traces to:** L1-008

Administrators must configure prize names and descriptions, with each prize entry representing one award. The eligible pool must contain active participants who successfully accessed the event before the draw and have not won in that event. Duplicate display names must remain distinct identities.

**Acceptance Criteria:**

1. Given accessed participants A and B, never-accessed C, deactivated D, and prior winner E, when eligibility is evaluated, then only A and B enter the draw.
2. Given a participant who won in another event, when eligible for this event, then the other event's win does not exclude them.
3. Given no prizes or an empty eligible pool, when a draw is requested, then an explanatory empty state appears and no award is recorded.
4. Given an awarded prize, when editing or removing that award is attempted, then its winner and awarded prize details remain immutable; future unawarded prizes remain editable.

---

## L2-021: Fair and atomic raffle results

**Traces to:** L1-008

The server must select uniformly from the eligible participant identities using secure randomness and atomically save the draw, prize, and winner before clients animate. The award must be final; replaying an animation must not redraw. Concurrent draws must not award one participant twice in the same event or one prize twice.

**Acceptance Criteria:**

1. Given one eligible participant, when an administrator draws an available prize, then that participant wins and is excluded from every subsequent event draw.
2. Given multiple eligible identities and a controlled randomness source in an integration test, when each possible selection index is exercised, then each identity can be selected exactly once across those fresh test fixtures; display order or profile fields do not change eligibility weights.
3. Given simultaneous draw requests for the same or different prizes, when committed, then each prize has at most one award and each participant at most one event win.
4. Given a committed draw whose response or animation was interrupted, when the action is retried or the page refreshed, then the original winner is returned without consuming another prize or choosing another person.

---

## L2-022: Raffle animation, audio, and celebration

**Traces to:** L1-008

With WebGPU available and reduced motion off, the raffle screen must cycle eligible participant names, play playful effects after the user enables sound, settle on the saved winner within ten seconds, and celebrate with particles. The winner must also be presented as accessible text and remain visible after effects finish.

**Acceptance Criteria:**

1. Given a saved result and available WebGPU, when the draw is presented, then names cycle and particle effects celebrate exactly the persisted winner; visual randomness never changes the result.
2. Given unsupported WebGPU, denied adapter access, or device loss during animation, when the result is presented, then a lightweight non-WebGPU presentation reveals the same winner within ten seconds without blocking later draws.
3. Given reduced motion enabled, when a draw completes, then the winner appears without rapid cycling or particles; given sound has not been enabled or is muted, then no audio plays.
4. Given a late joiner after the draw, when they open the raffle screen, then they see the saved winner without an automatic replay of sound or the draw animation.

---

## L2-023: Demo schedule

**Traces to:** L1-009

Administrators must create and reorder demo slots containing team, project, start time, and positive duration inside the demonstration interval. Slots must not overlap, and a team must appear at most once. Participants must see ordered slots with the current slot highlighted using server time.

Here the demonstration interval means the configured demo-presentation window in L2-006, not an entire named phase that includes preparation. A slot must reference a same-event team and its currently selected project; different teams may demonstrate the same project. Start is inclusive and end exclusive; gaps show no current presenter. For a reorder, retain each slot's team and duration, then pack slots consecutively from the earliest existing slot start in the requested order. Show the proposed times before explicit save; reject an overrun atomically. Administrators can subsequently edit individual times to introduce gaps. Removing a slot requires confirmation and affects only the lineup, not the team's project or links.

**Acceptance Criteria:**

1. Given valid configured slots, when participants view the schedule, then team/project names and times appear in start-time order and the active slot is highlighted.
2. Given overlapping slots, a duplicate team, or a slot outside the demonstration interval, when saved, then the change is rejected with the conflicting slot identified.
3. Given no slots, when the schedule is viewed, then an unscheduled-demo message appears; an administrator's valid update becomes visible within two seconds.
4. Given slots A at 20:30 for three minutes and B at 20:35 for four minutes, when an administrator previews and saves order B then A, then B starts at 20:30 and A at 20:34 with their original durations; cancellation preserves the original times.
5. Given a gap between slots or the exact end of the final slot, when the participant views the schedule, then no slot is marked current and the next start or lineup-complete message is shown.
6. Given a slot outside the demo-presentation window or referencing an unselected project, when saved, then the affected slot and reason are identified and the previously valid lineup remains active.

---

## L2-024: Team repository and demo links

**Traces to:** L1-009

Team members and administrators must save optional repository and demo URLs on the team's event project entry, so multiple teams choosing one project can showcase different builds. Team members can edit these links until event end; administrators can correct them afterward. This version stores links and does not host repositories or uploaded demo videos.

**Acceptance Criteria:**

1. Given a team member during the event, when valid HTTPS repository/demo links are saved, then their team's details and showcase display them without changing another team's links for the same selected project.
2. Given no links, when details render, then missing-link text appears without empty clickable actions; adding one link does not require the other or a Liturgy link.
3. Given another team's participant, an invalid URL, or an event that ended, when links are edited, then the participant change is denied and saved links remain unchanged; administrators can correct valid links after closing.

---

## L2-025: Showcase and event recap

**Traces to:** L1-009

The showcase must list event teams, chosen project descriptions, and supplied repository/demo links. The event recap must become the default client screen at event end, include configured closing content and showcase entries, and remain available to active event registrations. Missing demos must not prevent other projects from appearing.

**Acceptance Criteria:**

1. Given server time reaches event end, when the client transitions, then it shows closing content and all team entries, explicitly identifying teams without a selected project or demo link.
2. Given an active registration returning after the event, when authenticated, then the same saved showcase and current corrected links are accessible without reopening timed activities.
3. Given Use Liturgy disabled or enabled, when the recap renders, then its companion links obey L2-026 and L2-027 while repository/demo links remain available in both cases.

---

## L2-026: Event-level Liturgy opt-out

**Traces to:** L1-010

Use Liturgy must default to false for every new event and the September 9 preset. Only administrators must be able to change it. Disabling it must hide all participant-facing companion links, actions, onboarding, and recap content and omit Liturgy URLs from participant responses while retaining saved URLs for administrators.

**Acceptance Criteria:**

1. Given a new event or the September 9 preset, when participants use any core activity, then no Liturgy account, workspace, connectivity, onboarding, or link is required or displayed.
2. Given saved project URLs and integration enabled, when an administrator disables it, then connected participant screens remove companion content within two seconds and subsequent participant responses omit those URLs.
3. Given integration was disabled, when the administrator enables it again, then the previously saved project links are restored without re-entry; repository/demo links and activity data remain unchanged throughout.
4. Given a participant directly attempting to change Use Liturgy, when submitted, then the server denies the action and the event setting is unchanged.

---

## L2-027: Optional project links to Liturgy

**Traces to:** L1-010

Administrators must attach or clear an optional HTTPS Liturgy project URL for each event project. When enabled, linked projects must expose an "Open project in Liturgy" action on project-selection and project-detail screens and a companion link beside repository/demo links in recap. Link targets must not require a server-side connectivity check to save or display.

**Acceptance Criteria:**

1. Given integration enabled and a project URL saved, when each named screen renders, then its action opens the configured URL and leaves sign-in and membership enforcement to Liturgy.
2. Given integration enabled but no project URL, when the project renders, then no empty companion action appears and selecting/building the project still works.
3. Given projects at two events using the same Liturgy URL, when both are saved, then both links remain valid independent configurations without a uniqueness error or shared event data.
4. Given Liturgy is unreachable or rejects workspace access, when the event participant continues, then event selection, quizzes, chat, raffles, and demos remain available without waiting for companion recovery.

---

## L2-028: Companion responsibility boundaries

**Traces to:** L1-010

The connection must consist of navigation links only. The event platform must not create Liturgy projects, synchronize membership, share authentication, advance gated phases, complete requirements, or store ongoing boards, work items, assignments, or 5R progress. Event guidance can describe the process without recording project progress.

**Acceptance Criteria:**

1. Given a team joins or selects a linked project, when that event action completes, then no Liturgy membership or project-creation request occurs and the client explains that workspace access is separate when offering the link.
2. Given the schedule moves into Develop, when the new event screen appears, then no request advances Liturgy phase progress or completes Discover/Discern requirements.
3. Given all Liturgy network traffic is unavailable, when the full event journey is exercised with integration either off or on, then all core event actions succeed; only explicitly following an external link depends on Liturgy.

---

## L2-029: Standalone design-system delivery

**Traces to:** L1-011

The repository must deliver a self-contained design-system static site alongside the applications, with its own package manifest, tests, build, and deployment. Runtime assets, icons, styles, and any distributed font files must be licensed local copies owned here; the Cornerstone system-font stacks require no remote font service. No shared source, package, asset mount, or build/runtime dependency on Cornerstone or Liturgy is allowed in this version. Structural constraints are verified by review, not architecture tests.

**Acceptance Criteria:**

1. Given a clean checkout with declared build dependencies installed and no Cornerstone or Liturgy repository/deployment access, when the design-system site and both applications are built and served, then their screens render with system or locally distributed fonts, local icons, and local styling.
2. Given only the design-system static output deployed and the application API unavailable, when its documented components and states are explored, then examples and interactions work without application services.
3. Given network requests captured while those sites render, when their visual assets load, then none requires Cornerstone or Liturgy origins or remotely shared assets; the independently deployed gallery requires no event-application origin.

---

## L2-030: Authoritative local design tokens

**Traces to:** L1-011

The design-system site must own authoritative CSS custom properties under the local prefix `--cs-`, preserving the recorded Cornerstone light primitive values; the frontend must mirror those values. Every component stylesheet must consume tokens for colours, dimensions, fonts, spacing, radii, borders, shadows, and motion. Missing event-composition values must be added to the local design system first under the same prefix without changing reference primitive values. Token naming and absence of hard-coded values are review constraints.

**Acceptance Criteria:**

1. Given equivalent components in the site and both applications, when rendered with identical inputs, then computed visual values match the authoritative local tokens and the corresponding recorded Cornerstone reference values.
2. Given an event logo, venue branding, or content change, when rendered, then component typography, colour roles, spacing, and interaction styles remain those of the design system.
3. Given a token updated in a review fixture and its frontend mirror, when equivalent components render, then both applications and the site reflect that value consistently without component-specific overrides.

---

## L2-031: Cornerstone light visual and interaction parity

**Traces to:** L1-011

Design review must use the recorded Cornerstone light reference: its token/theme styles and equivalent rendered components from `C:/projects/Cornerstone`. It must record the source revision, relevant file paths and any working-tree differences, and local comparison captures. Compare token values, typography, colour, spacing, sizing, borders, radii, shadows, icons, component states, and motion; the deck and Liturgy are not styling overrides.

The adopted artifact reference is Cornerstone revision `554636edf74af788e83aa33a4380a05b031db16a`, documented in the [local design-system provenance and component map](../../design-system/README.md). Reference sources include `projects/cornerstone/src/styles/tokens.scss`, `theme.scss`, and the mapped component styles. This records provenance, not completed full visual acceptance. Preserve the reference specimens and comparison evidence locally so builds never require that checkout. Event layouts can compose these primitives; the all-light host navigation is a deliberate adaptation instead of the reference's inverted shell. Any additional accessibility conflict must be resolved explicitly under L2-036.

The September 7 inspection also found untracked card and choice-card component sources in the Cornerstone checkout. A commit identifier alone does not identify such sources. Design evidence must include snapshots or content hashes for every consulted uncommitted source; the existing local reference fixture preserves representative styles only and does not establish parity for the whole library.

**Acceptance Criteria:**

1. Given matching content, viewport, browser, device scale, and locally loaded fonts, when equivalent reference and event components are compared side by side in default, hover, focus, active, disabled, loading, error, and selected states where applicable, then no unexplained appearance or behavior difference remains before acceptance.
2. Given event phase labels and host navigation, when rendered, then phases remain identifiable by text and styles consume local Cornerstone tokens; navigation stays light, and no Liturgy/deck-specific phase palette overrides the primitives.
3. Given mockups, the standalone site, and both production applications, when reviewed with Use Liturgy false and true, then the same parity checks pass in both settings.

---

## L2-032: Light theme in every experience

**Traces to:** L1-011

All participant, administration, design-system, and mockup screens must use the recorded Cornerstone light-theme reference regardless of browser or operating-system theme. No dark mode or theme toggle must be offered. Dark ink accents that belong to the light reference are not a separate dark theme.

**Acceptance Criteria:**

1. Given light and dark operating-system preferences, when each screen and its overlays, forms, and feedback states render, then both use identical light-theme tokens and native control colour treatment.
2. Given a preference changes while a page is open, when the change is detected, then no theme change or theme toggle appears.
3. Given a previous dark-theme value in browser storage, when any application or the site opens, then that value does not activate a dark theme.

---

## L2-033: Prepare for later Cornerstone adoption

**Traces to:** L1-011

The agreed future direction is npm-based `@cornerstone/ui` adoption by both applications. Current component interface design must consult the [Cornerstone API and migration guidance](https://github.com/QuinntyneBrown/Cornerstone#readme), while retaining independent local implementations. Cornerstone publication, installation, and migration are outside this version. Package availability and compatibility must be verified before a future migration, independently of Use Liturgy.

**Acceptance Criteria:**

1. Given equivalent local button and card designs, when their design review is completed, then review evidence identifies the consulted Cornerstone equivalents and preserves the current light-reference behavior without adding a package dependency.
2. Given the current application build without Cornerstone installed, when built and served, then its local components remain functional and visually consistent with L2-031.
3. Given a future adoption proposal, when assessed for readiness, then documented package availability, compatibility with both applications, independent builds, and appearance/behavior parity are required before adoption is accepted.

---

## L2-034: Chrome and capability support

**Traces to:** L1-012

Acceptance must target the latest stable Google Chrome available on the verification date, recording its exact version and test operating system. Optional GPU and audio capabilities must be detected at runtime; access to event functionality must not depend on a successful GPU initialization or an audio permission grant.

**Acceptance Criteria:**

1. Given the recorded latest stable Chrome version, when the participant and administrator journeys are exercised, then authentication, navigation, timed activities, and saving work without browser errors that prevent completion.
2. Given WebGPU is unavailable or audio playback is denied, when any activity opens, then its controls and textual outcomes remain usable and the raffle follows L2-022.
3. Given a page is backgrounded across a stage boundary, when it returns to the foreground, then server state is refreshed and the current activity appears within two seconds of restored connectivity.

---

## L2-035: Responsive screens and states

**Traces to:** L1-012

Every client, admin, and design-system screen must work in XS (<576px), S (576-767px), M (768-991px), L (992-1199px), and XL (>=1200px) widths. Below 576px, primary content must use one column and navigation must remain accessible in a compact form. Layout must preserve access to content and actions at larger widths without requiring a particular column count.

**Acceptance Criteria:**

1. Given widths 320, 575, 576, 767, 768, 991, 992, 1199, 1200, and 1440 CSS pixels, when each screen's relevant loading, empty, populated, error, and overlay states render, then text and controls do not overlap or clip and the page requires no horizontal scrolling.
2. Given a wide data table at a narrow width, when viewed, then its labeled container can scroll horizontally without moving the entire page and all row actions remain reachable.
3. Given a mobile viewport and an open software keyboard, when a form or message is edited, then the active field, validation feedback, and submit action remain reachable through scrolling.
4. Given 200% browser zoom, when participant and admin workflows are completed, then navigation, dialogs, and primary actions remain visible or reachable without lost content.

---

## L2-036: Keyboard, semantics, and readable feedback

**Traces to:** L1-012

All interactive controls must have accessible names, visible focus, and keyboard operation. Dialogs must contain focus and return it to their trigger on dismissal. Errors must be associated with fields and announced; status changes must use accessible announcements without repeatedly reading every countdown tick. Normal text must have at least 4.5:1 contrast and large text and meaningful control boundaries at least 3:1.

**Acceptance Criteria:**

1. Given keyboard-only input, when access, team/project selection, profile editing, chat, quizzes, admin configuration, and raffle controls are exercised, then all actions complete without pointer input or focus traps.
2. Given an invalid submission or modal dismissal, when feedback appears or the modal closes, then focus reaches the error summary or returns to the originating control respectively and the relevant message is accessible by name.
3. Given an automatic stage change, when the main event screen changes, then its new title is announced and focus moves to its heading if the previously focused element was removed; the countdown does not announce every second.
4. Given each reference-matched light-theme state, when contrast is measured, then it meets the stated ratios; any reference conflict must be explicitly reconciled during design review rather than silently weakening parity or readability.

---

## L2-037: Motion and sound controls

**Traces to:** L1-012

The site and applications must honor reduced-motion preferences and provide a participant-visible mute control for event effects. Sound must begin only after a deliberate enable-sound action and stop when muted. Essential information must never be conveyed only through motion, sound, or colour.

**Acceptance Criteria:**

1. Given reduced motion on, when transitions or celebrations occur, then nonessential movement is removed while the resulting stage, answer result, or winner remains available as text.
2. Given sound has not been enabled, when a page loads or a timed activity begins, then audio remains silent; after enabling sound, muting stops active and future effects.
3. Given animation or sound fails, when an activity completes, then accessible text and controls expose the same result and next action.

---

## L2-038: Administrator authentication and authorization

**Traces to:** L1-013

Administration must require authentication to an operator-provisioned account with the admin role. There must be no public admin registration, default production password, or participant-driven role grant. Authorization must be enforced for every administration read and mutation, including direct requests. Administrator sessions must expire after 30 minutes of inactivity or eight hours total and support sign-out.

**Acceptance Criteria:**

1. Given an unauthenticated requester or authenticated participant, when any administrator operation is requested, then no privileged data or change is returned and authentication or authorization is rejected respectively.
2. Given a valid administrator, when event configuration or moderation is performed, then the requested authorized operation succeeds and is auditable under L2-045.
3. Given an expired, signed-out, or role-revoked administrator session, when its next privileged request arrives, then it fails without changing data; altering a client-side role value never restores access.
4. Given a fresh production deployment, when an unknown account attempts administrative access, then no bundled default credential or participant entry code grants the admin role.

---

## L2-039: Event and ownership boundaries

**Traces to:** L1-013

Every server operation must validate the requester's event membership and resource ownership, independent of client routing. Profiles, messages, submissions, credentials, and moderation reports must expose only the fields authorized for that actor. Admin event management authority must not grant ordinary browsing of unreported private conversations.

**Acceptance Criteria:**

1. Given a participant in event A, when an event B identifier is substituted into profile, team, project, quiz, raffle, or recap requests, then no B data is disclosed or changed.
2. Given a participant outside a conversation, when they request its messages or attempt to send as another participant, then access is denied even if they know the identifiers.
3. Given public-to-event profile, leaderboard, or winner results, when responses are inspected, then email, entry credentials, session credentials, and private answers are absent.
4. Given a request for an inaccessible resource, when refused, then the response does not disclose its owner's identity or private contents.

---

## L2-040: Input, links, and uploaded branding

**Traces to:** L1-013

The server must validate all content and settings. Names/titles must be 1-200 characters after trimming, biographies/descriptions/guidance at most 5,000, tags at most 40 characters each and 20 per profile, and URLs at most 2,048. Optional blank values mean absent. Submitted links must be absolute HTTPS URLs without embedded credentials; scripts, executable markup, and arbitrary CSS must not be accepted as event content. Venue logos must accept PNG, JPEG, or WebP up to 2 MiB and 4,096 pixels per dimension after decoding; executable or mismatched uploads must be rejected.

**Acceptance Criteria:**

1. Given valid values at each documented limit and invalid values one character, byte, or pixel above it, when saved, then boundary values succeed and oversized values fail with field-specific errors without partial writes.
2. Given script markup, event-handler attributes, SQL-like text, or executable URL schemes in participant/admin inputs, when submitted or rendered, then no script or query executes and invalid fields are rejected or displayed only as inert text.
3. Given a disguised executable, invalid image, SVG, or over-limit logo, when uploaded, then it is rejected without becoming a public executable asset; an accepted image renders as an image.
4. Given a user-supplied external URL, when saved or viewed, then the API does not fetch it or expose internal-network content; following a link opens only the validated target without giving it control of the originating page.

---

## L2-041: Credentials, transport, and private state

**Traces to:** L1-013

Production traffic must use HTTPS. Entry codes must be generated with at least 128 bits of cryptographic entropy, stored as nonrecoverable verifiers, and revealed only on issuance/replacement to administrators. Authentication secrets must not enter URLs, analytics, application logs, or browser local storage. Authenticated state changes must reject forged cross-site requests, and sign-out must remove displayed private state.

**Acceptance Criteria:**

1. Given a newly issued code, when an administrator later reads the roster, then the original code is not retrievable; replacing it creates a different credential and invalidates the old one.
2. Given participant/admin login and protected actions in production configuration, when browser traffic and persistence are inspected, then credentials are sent only over HTTPS and are absent from URLs and local storage; session cookies, if used, are Secure and HttpOnly.
3. Given an untrusted origin attempting an authenticated mutation with browser-held credentials, when the request reaches the API, then it is denied without a data change.
4. Given private content was previously displayed, when the user signs out and navigates back, then the content is not restored from application state or a shared response cache.

---

## L2-042: Abuse limits and actionable denials

**Traces to:** L1-013

Authentication must allow at most ten failed attempts per source address per rolling five minutes and five per submitted code verifier per rolling five minutes. Failed admin sign-ins must also use the address limit and a five-failure account limit. Chat sends must allow at most 30 accepted messages per sender per rolling minute. Limits must persist across application instances; denials must be generic for authentication and must not permanently lock an account.

**Acceptance Criteria:**

1. Given the relevant failed-login limit has been reached, when the next attempt arrives before the window expires, then it is rejected with a retry delay without testing or revealing the credential's validity.
2. Given 30 accepted messages within a minute, when the sender attempts a 31st, then it is refused with a retry delay and retained as an unsent draft; another sender's allowance is unaffected.
3. Given the limiting window has elapsed, when a valid request is repeated, then normal processing resumes without administrator intervention.
4. Given authentication attempts using nonexistent and existing identities with wrong credentials, when responses are compared, then neither response exposes whether a roster binding or administrator account exists.

---

## L2-043: Measurable performance baseline

**Traces to:** L1-014

The baseline must support 200 connected participants and two administrators in one event. Measure after two minutes of warm-up for ten minutes on a recorded reference deployment, with each participant issuing one read every five seconds and one valid mutation every 30 seconds, administrators making one configuration change per minute, and a scheduled transition and raffle during the run. Measure API latency at the client; include normal network transit and exclude third-party map/link loads. Record deployment resources, database, browser version, network conditions, and raw results for reproducibility.

**Acceptance Criteria:**

1. Given the baseline load with valid inputs, when measured, then aggregate API p95 is at most 1,000 ms, unexpected server/network failures affect at most 1% of requests, and no acknowledged mutations are lost; intentional validation, rate-limit, and conflict responses are reported separately.
2. Given connected clients under baseline load, when stages change or configuration and messages are saved, then every connected observed client displays the applicable update within two seconds.
3. Given 200 eligible participants attempting quiz submission within one second, when processed, then each participant's valid first answer is recorded once and API p95 remains at most 1,000 ms.
4. Given the baseline repeated for a second ten-minute interval, when compared, then the same latency and delivery targets still hold without application restart or manual cleanup.

---

## L2-044: Persistence, interruption, and recovery

**Traces to:** L1-014

The server must acknowledge success only after durable persistence. Roster bindings, event settings, teams/projects, profiles, messages, quiz submissions/results, reports, and raffle awards must survive application restart. The client must identify disconnected or failed states, retain unsent drafts in memory, and reconcile with authoritative state when connectivity returns. Conflicting administrator and shared team edits must reject stale writes with a reload/reapply action rather than silently overwrite newer changes.

**Acceptance Criteria:**

1. Given acknowledged changes across the listed data, when the application restarts without deleting durable storage, then rereading returns those changes and current stage is recomputed from server time.
2. Given storage fails before commit, when a mutation is attempted, then no success is shown, no partial result is exposed, and a retry cannot duplicate an eventual award, membership, answer, or message.
3. Given a participant disconnected across a boundary, when the connection returns, then authentication is rechecked, the current stage and saved activity state load within two seconds after a successful synchronization response, and unsent drafts are not submitted automatically.
4. Given two editors saving from the same version, when the first save succeeds, then a conflicting second save is rejected with the current values and a reapply action; unrelated valid edits remain usable.
5. Given a documented database backup from a test event, when restored into an isolated environment, then saved registrations, selections, messages, quiz results, and raffle history from the backup are readable and coherent; the runbook states backup frequency and recovery steps.

---

## L2-045: Operational diagnostics and audit

**Traces to:** L1-014

Operators must have protected access to application readiness, request latency and failure metrics, and structured failure records with correlation identifiers. Administrative configuration, access revocation, moderation, and raffle actions must record actor, event, action, time, and outcome. Diagnostic records must exclude message bodies, biographies, participant emails, submitted codes, and authentication secrets; audit records use stable identifiers. Deployment documentation must explain configuration, secret provisioning, startup, backup/restore, and recovery verification.

**Acceptance Criteria:**

1. Given a failed request, when the client displays an error reference, then an operator can correlate it to a server failure record without exposing internal exception details to the participant.
2. Given unavailable durable storage, when readiness is checked, then the application reports not ready; after restoration it reports ready and normal requests resume.
3. Given each audited administrator action, when performed, then one attributable outcome is recorded; when logs are inspected using distinctive test email, message, and credential values, then those sensitive values are absent.
4. Given an operator following the deployment runbook in a clean environment, when services start with valid configuration, then readiness and a test participant journey succeed; missing required configuration produces an actionable operator error without leaking secret values.

## L2-046: Event entry and participant navigation

**Traces to:** L1-002

An event-specific link must identify the selected event before participant authentication and show its published title and access form. A public directory of events is not required. After authentication, entry must open the countdown, current stage/waiting content, or recap according to authoritative event time. Protected deep links must recheck the selected event's session; after authentication they must return to the requested accessible resource. Core navigation must offer the current event screen, schedule, teams/projects, people/profile, messages, quizzes, raffle, showcase, and sign-out without changing mutation windows.

**Acceptance Criteria:**

1. Given an event link and no session, when it opens, then the access form identifies that published event without revealing its roster or credentials; an unknown or draft event shows a generic unavailable response.
2. Given a protected same-event project link and no session, when authentication succeeds, then the requested project opens if still accessible; a missing resource shows an unavailable state with a return-to-event action and no private owner details.
3. Given a participant browsing a core activity, when they choose the current-event action, then the authoritative current screen opens; browsing a closed activity never enables its closed mutations.
4. Given access to event A and a link for event B, when B opens, then B requires its own valid session and never displays cached A content as B content.

---

## L2-047: Event list and configuration reuse

**Traces to:** L1-001

Administrators must list and reopen events with title, date, timezone, and draft/published status; blank creation and copying an existing event's configuration must be available. A copy must receive a new event identity, remain a draft, and default Use Liturgy to false while retaining configured companion URLs. Copy content, venue configuration, team definitions, project choices, stage/activity definitions, quiz questions, and unawarded prize definitions. Never copy registrations, credentials, profiles, memberships, team/project selections or build links, messages, blocks, reports, submissions, awards, or demo slots. Copies must reset activity lifecycle state. Administrators must supply the new event start; shift configured intervals by the same elapsed duration and validate timezone resolution before saving.

**Acceptance Criteria:**

1. Given published and draft events, when an administrator opens the event list and selects either, then its status and saved configuration appear without another event's data.
2. Given a populated event with Use Liturgy enabled, when its configuration is copied to a valid new start, then the new draft has shifted intervals and retained configuration/companion URLs, Use Liturgy false, and none of the excluded participation data.
3. Given a copied event, when its settings or projects change, then the original event remains unchanged; a published source's credentials do not grant entry to the copy.
4. Given blank event creation, when the incomplete event is saved, then it is a draft with empty participant/activity collections and Use Liturgy false, subject to L2-001 validation.

---

## L2-048: Participant directory and profile discovery

**Traces to:** L1-006

Authenticated participants must browse active same-event roster names and search by name or shared skills, interests, and help tags. Search must trim the query and match case-insensitive substrings; a blank query lists all active entries, ordered by case-insensitive display name then stable identity. Optional fields must appear only while sharing is enabled. A private profile must show the roster name and a not-shared explanation, never generated personal details. Sharing is not a prerequisite for being a chat recipient; either-direction blocks still prevent sending under L2-016.

**Acceptance Criteria:**

1. Given a matching name or shared tag, when its trimmed query is searched with different casing, then the active same-event participant appears; duplicate display names remain separately selectable identities.
2. Given private optional fields, when another participant searches those fields or opens the profile, then the fields neither produce matches nor appear in the result/profile; the roster name remains discoverable.
3. Given no active entries or no matches, when the directory renders, then it explains the empty result and permits clearing the search without inventing participants.
4. Given a deactivated or other-event entry, when its name is searched or its profile directly requested, then no profile details are disclosed; conversation history access continues to follow L2-015 and L2-016.

---

## Acceptance delivery and review

The [L1 traceability matrix](L1.md#traceability-matrix) maps all 48 detailed requirements to the 14 capabilities. Every numbered criterion is an acceptance obligation. The requirements documents define expected behavior; they do not assert that an application, performance baseline, or design comparison has already passed.

| Verification method | Required evidence |
| --- | --- |
| API integration acceptance | Real API and isolated persistence; access binding, authorization, schedule boundaries, team/project conflicts, chat privacy, quiz scoring, raffle atomicity, validation, and restart recovery. |
| Playwright acceptance | One page object per screen; tests express intent without selectors; composition injects mock service contracts and never reaches production HTTP adapters. Exercise time, errors, empty states, and simultaneous-update results through controlled fixtures. |
| Responsive and accessible interaction | Every relevant screen/state at the L2-035 widths, keyboard completion, focus restoration, announcements, light preference invariance, reduced motion, mute, and unavailable WebGPU/audio. |
| Design acceptance | Side-by-side reference captures and review of equivalent states, plus functional examples on the independent design-system site. Review token ownership, naming, and component placement without architecture tests. |
| Performance and operational acceptance | Recorded load conditions and timings against the real API, burst quiz submissions, durable restart/retry cases, isolated backup restore, readiness, and redacted diagnostic evidence. |

Tests must include the L2 trace comment header specified in L1. Test implementations must begin failing against their selected behavior, then pass after a verified incremental implementation. Test fixtures must use synthetic names, emails, messages, and credentials. No test must parse these documents or scan code layout to enforce traceability or architecture.

Documentation completion consists of reviewing unique sequential IDs, one valid parent per L2, coverage for every L1 and prompt capability, concrete Given-When-Then criteria, approved-default provenance, and absence of unfinished placeholders. Those checks are editorial review, not an application test suite.
