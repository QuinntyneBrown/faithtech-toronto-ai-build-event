export const esc = (value = '') => String(value).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export const url = (screen, params = {}) => '?' + new URLSearchParams({ screen, ...params });
export const link = (screen, label, style = 'secondary', params = {}) => `<a class="cs-button cs-button--${style}" href="${url(screen,params)}" data-nav>${label}</a>`;
export const button = (action, label, style = 'primary', extra = '') => `<button type="button" class="cs-button cs-button--${style}" data-action="${action}" ${extra}>${label}</button>`;
export const dialogLink = (id, label, item = '', style = 'secondary') => button('dialog', label, style, `data-dialog="${id}" data-item="${esc(item)}"`);
export const field = (name,label,value='',type='text',hint='',required=true) => `<label class="cs-field"><span class="cs-label">${label}${required ? ' <span aria-hidden="true">*</span>' : ''}</span><input class="cs-input" name="${name}" type="${type}" value="${esc(value)}" ${required ? 'required' : ''}><span class="cs-hint">${hint}</span></label>`;
export const area = (name,label,value='',required=true) => `<label class="cs-field"><span class="cs-label">${label}</span><textarea class="cs-textarea" name="${name}" rows="3" ${required?'required':''}>${esc(value)}</textarea></label>`;
export const select = (name,label,options,value) => `<label class="cs-field"><span class="cs-label">${label}</span><select class="cs-select" name="${name}">${options.map(o=>{const [v,l]=Array.isArray(o)?o:[o,o];return `<option value="${esc(v)}" ${v===value?'selected':''}>${esc(l)}</option>`;}).join('')}</select></label>`;
export const check = (name,label,checked=false,hint='') => `<label class="choice"><input type="checkbox" name="${name}" ${checked?'checked':''}><span><strong>${label}</strong>${hint?`<br><span class="muted">${hint}</span>`:''}</span></label>`;
export const pill = (label, live=false) => `<span class="cs-pill ${live?'badge-live':''}">${esc(label)}</span>`;
export const title = (eyebrow,heading,description,actions='') => `<div class="page-heading"><div><p class="eyebrow">${eyebrow}</p><h1>${heading}</h1><p class="lede">${description}</p></div>${actions?`<div class="cs-cluster">${actions}</div>`:''}</div>`;
export const card = (body,classes='') => `<article class="cs-card ${classes}">${body}</article>`;
export const alert = (text,tone='info') => `<div role="${tone==='error'?'alert':'status'}" class="cs-alert cs-tone--${tone}">${text}</div>`;
export const empty = (heading,description,action='') => `<div class="empty"><p class="eyebrow">A LITTLE SPACE FOR POSSIBILITY</p><h2>${heading}</h2><p class="muted">${description}</p>${action}</div>`;
export const person = p => `<div class="person"><span class="avatar" aria-hidden="true">${esc(p.name.split(' ').map(n=>n[0]).join('').slice(0,2))}</span><div><strong>${esc(p.name)}</strong><p class="muted">${esc(p.role||p.skills||'Participant')}</p></div></div>`;
export const table = (caption,heads,rows) => `<div class="cs-table-container"><table class="cs-table"><caption>${caption}</caption><thead><tr>${heads.map(h=>`<th scope="col">${h}</th>`).join('')}</tr></thead><tbody>${rows.join('')}</tbody></table></div>`;
export const external = (href,label) => /^https?:\/\//.test(href) ? `<a class="text-link" href="${esc(href)}" target="_blank" rel="noopener noreferrer">${label} ↗</a>` : `<span class="muted">${label} not added yet</span>`;
export const form = (id,body,submit='Save changes',extra='') => `<form data-form="${id}" ${extra}><div class="form-error" role="alert"></div>${body}<div class="form-actions"><button class="cs-button" type="submit">${submit}</button></div></form>`;
